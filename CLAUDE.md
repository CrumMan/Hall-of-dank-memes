# Project Instructions

## Note-taking convention

When the user's message starts with (or contains) `NOTE:`, take the text that follows it and append it as a new bullet line to `NOTE.md` in this folder, instead of just replying in chat. Create `NOTE.md` if it doesn't already exist.
